# Base URL of the backend API.
# Local development: the Vite dev-server proxies /api, so the default works.
# Production (Vercel): set to the deployed backend, e.g.
#   VITE_API_BASE_URL=https://ev-charging-dss.up.railway.app/api
VITE_API_BASE_URL=http://localhost:8000/api
