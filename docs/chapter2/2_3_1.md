## 2.3. Розробка алгоритмів функціонування системи

У підрозділі формалізовано алгоритми обчислювального ядра. Кожен з трьох методів гібридної схеми (математика – у 1.2.2–1.2.4) поданий у двох формах: UML Activity Diagram і структурний псевдокод. Завершують підрозділ діаграма активностей загального сценарію та діаграма розгортання.

### 2.3.1. Алгоритм Fuzzy AHP

Алгоритм реалізує формули (1.3)–(1.9) підрозділу 1.2.2; для контролю узгодженості суджень застосовано також коефіцієнт $CR$ за формулою (1.2). Вхід: нечітка матриця попарних порівнянь $\tilde{A} = [(l_{ij}, m_{ij}, u_{ij})]_{m \times m}$. Вихід: нормалізований вектор ваг $W = (w_1, \ldots, w_m)^T$ із $\sum_{j=1}^{m} w_j = 1$. Алгоритм виконується у сім кроків:

1. Формування точкової матриці $A_{\text{crisp}} = [m_{ij}]$ з модальних значень TFN.
2. Обчислення максимального власного значення $\lambda_{\max}$ матриці $A_{\text{crisp}}$.
3. Обчислення коефіцієнта узгодженості $CR$ за формулою (1.2) з використанням $\lambda_{\max}$ і табличного випадкового індексу $RI_m$.
4. Контроль узгодженості: якщо $CR > 0{,}1$ – завершення з повідомленням ОПР; якщо $CR \leq 0{,}1$ – продовження.
5. Обчислення нечітких синтетичних екстентів $S_i$ за формулою (1.7).
6. Обчислення ступенів можливості $V(S_i \geq S_k)$ за (1.8) і мінімальних ступенів $d'(A_i)$ за (1.9).
7. Нормалізація $w_j = d'(A_j) / \sum_{l} d'(A_l)$ за формулою (1.9).

Графічне подання алгоритму у нотації UML Activity Diagram наведено на рис. 2.9.

![Діаграма активностей Fuzzy AHP. Вхід: нечітка матриця попарних порівнянь. Активності: формування точкової матриці модальних значень, обчислення λ_max, CI, CR. Гілка CR ≤ 0.1: «ні» – повідомлення ОПР про неузгодженість і завершення; «так» – обчислення синтетичних екстентів за (1.7), ступенів можливості за (1.8), мінімальних ступенів за (1.9), нормалізація. Вихід: нормалізований вектор ваг W](images/fig_2_9_fahp_activity.png)

Рис. 2.9. Діаграма активностей алгоритму Fuzzy AHP

<!-- PlantUML-джерело: images/sources/fig_2_9_fahp_activity.puml -->

**Алгоритм 2.1. Обчислення ваг критеріїв методом Fuzzy AHP**

```
Вхід:  Ã = [(l_ij, m_ij, u_ij)]_{m×m} – нечітка матриця попарних порівнянь
       RI[m] – табличний випадковий індекс узгодженості для розмірності m
Вихід: W = (w_1, …, w_m) – нормалізований вектор ваг

1:  for i ← 1 to m, j ← 1 to m do
2:      A_crisp[i, j] ← Ã[i, j].m              // модальне значення
3:  end for
4:  λ_max ← largest_eigenvalue(A_crisp)
5:  CI    ← (λ_max − m) / (m − 1)
6:  CR    ← CI / RI[m]
7:  if CR > 0.1 then
8:      return ERROR("Матриця неузгоджена: CR = " + CR + " > 0.1")
9:  end if
10: total ← zero_TFN()
11: for p ← 1 to m do
12:     for q ← 1 to m do
13:         total ← total ⊕ Ã[p, q]            // ⊕ – нечітке додавання за (1.4)
14:     end for
15: end for
16: total_inv ← inverse_TFN(total)               // (1/u_total, 1/m_total, 1/l_total)
17: for i ← 1 to m do
18:     row_sum ← zero_TFN()
19:     for j ← 1 to m do
20:         row_sum ← row_sum ⊕ Ã[i, j]
21:     end for
22:     S[i] ← row_sum ⊗ total_inv               // ⊗ – нечітке множення за (1.5)
23: end for
24: for i ← 1 to m do
25:     min_d ← +∞
26:     for k ← 1 to m, k ≠ i do
27:         v ← degree_of_possibility(S[i], S[k])    // (1.8)
28:         if v < min_d then min_d ← v
29:     end for
30:     d_prime[i] ← min_d                        // (1.9)
31: end for
32: sum_d ← Σ_{j=1..m} d_prime[j]
33: for j ← 1 to m do
34:     W[j] ← d_prime[j] / sum_d                 // (1.9)
35: end for
36: return W
```

Складність – $O(m^3)$ (домінує обчислення $\lambda_{\max}$); при $m = 10$ – близько $10^3$ елементарних операцій, що значно менше за часовий бюджет 5 с. Стійкість гарантує CR-контроль (крок 4: блокування за $CR > 0{,}1$) і нормалізація (крок 7: точна $\sum w_j = 1$).
