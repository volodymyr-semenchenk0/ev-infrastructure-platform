# План технічного боргу та підготовки даних (код)

Окремий план для коду, не пов'язаного з фічею порівняння профілів.
Інші плани: текст курсової – `plan_audit_2026_05_30.md`; порівняння профілів – `plan_profile_comparison_feature.md`.

```yaml
plan_meta:
  scope: "код – підготовка даних і технічний борг"
  обмеження:
    - "НЕ чіпати mcdm/ (математичне ядро завершене і верифіковане)"
    - "нетривіальні зміни – спершу plan mode, далі TDD"
    - "через Cowork коміти не робляться; git робить користувач сам"

пріоритет_0_критично:   # блокує числа Розділу 3
  - id: C0-1
    задача: "Підставити реальні дані замість синтетичних для експерименту"
    файли: ["app/backend/db/seed.py", "app/backend/db/defaults.py"]
    деталі:
      - "значення критеріїв для 12 локацій-кандидатів Києва"
      - "CRITERION_RANGES – фінальні значення замість плейсхолдерів"
      - "джерела даних за 2.2.3: OpenStreetMap, Open Charge Map, державна статистика"
    залежність_для: "обчислювальний експеримент 3.2.3 / 3.2.4 (див. plan_audit_2026_05_30.md)"
    перевірка: "після підстановки – прогін системи дає коректні ранжування для двох профілів"

пріоритет_1_коректність:
  - id: C1-1
    задача: "Виправити str/int-розбіжність ключів JSON у чутливості"
    файл: "app/backend/services/sensitivity_service.py"
    деталі:
      - "stability_matrix серіалізує ключі як str, DTO SensitivityRead очікує int"
      - "узгодити серіалізацію/десеріалізацію; покрити тестом"

пріоритет_2_чистка:
  - id: C2-1
    задача: "Прибрати або задокументувати мертві сутності"
    деталі:
      - "ExistingStation і PairwiseMatrixEntry не використовуються в сервісах/API"
      - "Supabase-конфіг у core/config.py неактивний"
      - "рішення: видалити чи лишити з явною позначкою плану використання"

пріоритет_3_низько:   # не блокує захист
  - id: C3-1
    задача: "Додати FK-індекси та індекс на evaluation_runs.created_at"
  - id: C3-2
    задача: "Переглянути backfill-логіку у міграції 0003_align_with_chapter2"
  - id: C3-3
    задача: "Дописати unit-тести UI (карта, чарти, кнопки експорту)"

verification:
  - "make all зелений (ruff + mypy --strict + pytest бекенд і фронтенд)"
  - "mypy --strict mcdm/ не змінюється"
  - "uvicorn і vite піднімаються"

рекомендована_послідовність:
  - "1. C0-1 (реальні дані) – першим, бо від нього залежать числа Розділу 3"
  - "2. C1-1 (коректність серіалізації чутливості)"
  - "3. C2-1, потім C3-* за наявності часу"
```
