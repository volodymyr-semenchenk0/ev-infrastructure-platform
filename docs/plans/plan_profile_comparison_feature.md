# План реалізації фічі порівняння профілів ОПР (код)

Окремий план для коду. Текстова частина курсової – у `plan_audit_2026_05_30.md`.
Фічу вирішено реалізувати повноцінно; у Розділі 3 вона згадується 1–2 абзацами, числові результати – у 3.2.4.

```yaml
feature_meta:
  name: "Порівняння профілів ОПР"
  scope: "код – бекенд + фронтенд + тести"
  залежність_для: "обчислювальний експеримент 3.2.3 / аналіз 3.2.4 курсової"
  обмеження:
    - "НЕ чіпати mcdm/ – математика Спірмена вже в services через scipy"
    - "нетривіальна зміна бекенду+фронтенду – спершу plan mode, погодити підхід, далі TDD (red → green → refactor)"
    - "через Cowork коміти не робляться; git робить користувач сам"
  поточний_стан_коду:
    - "є порівняння двох прогонів оцінювання: ComparisonService.compare, GET /evaluations/{a}/comparison/{b}, фронтенд useComparison"
    - "воно profile-agnostic: без міток профілів і без автоматичного канонічного прогону на профіль"

design_open_questions:   # погодити в plan mode ПЕРЕД кодом
  - "форма API: новий endpoint порівняння за двома profile_id, чи переюз наявного через два окремі прогони"
  - "канонічна матриця профілю – з build_default_pairwise_matrix (defaults.py)"
  - "форма відповіді: назви профілів, ваги і ранжування на кожен профіль, spearman_rho, попарні дельти рангів"
  - "чи персистувати результат порівняння профілів, чи рахувати ephemeral"

backend:
  - id: B-1
    задача: "Схема ProfileComparisonRead"
    деталі: ["мітки/назви профілів А та Б", "ваги і ранжування на кожен профіль", "spearman_rho", "попарні дельти рангів"]
  - id: B-2
    задача: "Сервіс порівняння профілів"
    деталі: ["для кожного профілю – канонічний прогін за дефолтною матрицею", "переюз ComparisonService.compare для rho і дельт", "без дублювання математики"]
  - id: B-3
    задача: "Endpoint порівняння профілів"
    деталі: ["оркестрація на рівні роутера, без математики", "404 на неіснуючий профіль", "422 на однакові профілі за потреби"]
  - id: B-4
    задача: "Integration-тести (httpx + PostGIS)"
    деталі: ["порівняння seed-профілів municipal vs investor", "перевірка rho, дельт і міток профілів"]

frontend:
  - id: F-1
    задача: "Хук useProfileComparison"
    деталі: ["вибір двох профілів, запит до нового endpoint, react-query"]
  - id: F-2
    задача: "Екран/секція порівняння профілів"
    деталі: ["вибір профілю А і Б, запуск, відображення результату"]
  - id: F-3
    задача: "Переюз компонентів порівняння з профільними підписами"
    деталі: ["GroupedBarChart, SpearmanBadge, DiffTable – назви профілів замість generic A/B"]
  - id: F-4
    задача: "Тести"
    деталі: ["unit на хук і компоненти", "e2e-сценарій порівняння профілів"]

verification:
  - "make all зелений (ruff + mypy --strict + pytest бекенд і фронтенд)"
  - "mypy --strict mcdm/ не змінюється (ядро не чіпаємо)"
  - "uvicorn і vite піднімаються; новий endpoint відповідає"
  - "mcdm-verifier не потрібен – математичне ядро поза скоупом"

послідовність:
  - "1. plan mode – погодити форму API і відповіді"
  - "2. TDD бекенд: тести → сервіс → endpoint"
  - "3. фронтенд: хук → секція → переюз компонентів → тести"
  - "4. верифікація: make all, ручна перевірка endpoint і UI"
```
